import React from 'react';

function LogonTitle(props) {
    return (
        <header className="form-header">
            <h2>{props.title}</h2>
        </header>
    );
}

export default LogonTitle;